import {useEffect,useState} from 'react';
import {Alert,StyleSheet,Text,View} from 'react-native';
import {GameButton} from './GameButton'; import {Panel} from './Panel'; import {C} from '../theme/theme';
import {onlineConfigured} from '../online/supabase'; import {guildApplications,guildRoster,myGuild,reviewGuildApplication,type GuildApplication,type GuildMember} from '../online/social';
export function OnlineGuildManagement(){const [members,setMembers]=useState<GuildMember[]>([]),[applications,setApplications]=useState<GuildApplication[]>([]),[role,setRole]=useState<'leader'|'officer'|'member'|null>(null),[busy,setBusy]=useState(false),[loaded,setLoaded]=useState(false);const load=async()=>{if(!onlineConfigured)return;setBusy(true);try{const mine=await myGuild();setRole(mine?.role??null);if(mine){setMembers(await guildRoster(mine.guild_id));setApplications(mine.role==='leader'||mine.role==='officer'?await guildApplications(mine.guild_id):[])}setLoaded(true)}catch(error){Alert.alert('Guild',error instanceof Error?error.message:'Unable to load your online guild.')}finally{setBusy(false)}};useEffect(()=>{load()},[]);if(!onlineConfigured||(!loaded&&!busy))return null;if(!role)return <Panel><Text style={s.title}>My guild · online</Text><Text style={s.sub}>You have not joined an online guild yet.</Text><GameButton title="Refresh my guild" tone="secondary" disabled={busy} onPress={load}/></Panel>;const review=async(id:string,accept:boolean)=>{setBusy(true);try{await reviewGuildApplication(id,accept);await load()}catch(error){Alert.alert('Guild application',error instanceof Error?error.message:'Unable to review application.')}finally{setBusy(false)}};return <Panel><Text style={s.title}>My guild roster · online</Text><Text style={s.sub}>Your role: {role}</Text>{members.map(member=><View key={member.account_id} style={s.member}><Text style={s.name}>{member.display_name}</Text><Text style={s.role}>{member.role}</Text></View>)}{(role==='leader'||role==='officer')&&<><Text style={s.section}>Pending applications</Text>{applications.length?applications.map(app=><View key={app.id} style={s.member}><Text style={s.name}>Applicant {app.account_id.slice(0,8)}</Text><View style={s.actions}><GameButton title="Accept" disabled={busy} onPress={()=>review(app.id,true)}/><GameButton title="Decline" tone="secondary" disabled={busy} onPress={()=>review(app.id,false)}/></View></View>):<Text style={s.sub}>No pending applications.</Text>}</>}<GameButton title="Refresh roster" tone="secondary" disabled={busy} onPress={load}/></Panel>}
const s=StyleSheet.create({title:{color:C.text,fontSize:18,fontWeight:'900',marginBottom:5},sub:{color:C.muted,lineHeight:19,marginBottom:8},section:{color:C.accent,fontWeight:'900',marginTop:10},member:{paddingVertical:8,borderTopWidth:1,borderColor:C.line},name:{color:C.text,fontWeight:'800'},role:{color:C.muted,fontSize:12},actions:{flexDirection:'row',gap:8,marginTop:6}});
