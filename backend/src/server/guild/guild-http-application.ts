import {parseGuildCreate,parseGuildProfileUpdate} from './guild-api-contracts';import {GuildApplicationService} from './guild-application';
export class GuildHttpApplication{constructor(private readonly service:GuildApplicationService){}self(accountId:string){return this.service.self(accountId)}directory(accountId:string,limit?:number){return this.service.directory(accountId,limit)}leaderboard(accountId:string,limit?:number){return this.service.leaderboard(accountId,limit)}create(accountId:string,body:unknown,nowMs:number){return this.service.create(accountId,parseGuildCreate(body),nowMs)}updateProfile(accountId:string,body:unknown,nowMs:number){return this.service.updateProfile(accountId,parseGuildProfileUpdate(body),nowMs)}allocateSkill(accountId:string,skillId:string,requestId:string,nowMs:number){return this.service.allocateSkill(accountId,skillId,requestId,nowMs)}contributeProject(accountId:string,characterId:string,optionId:string,requestId:string,nowMs:number){return this.service.contributeProject(accountId,characterId,optionId,requestId,nowMs)}}
