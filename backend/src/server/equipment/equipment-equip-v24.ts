import {EQUIPMENT_PIECES_V23,EQUIPMENT_SETS_V23} from './equipment-catalog-v23';import type {ActiveSetBonusV22,EquipmentSlot,GearStatKey} from './equipment-types-v22';import {resolveSetBonusesV22} from './equipment-set-resolver-v22';import {deriveInstanceV24,type EquipmentItemInstanceV24} from './equipment-item-instance-v24';
export type EquipmentLoadoutV24=Partial<Record<EquipmentSlot,string>>;
export interface EquipContextV24{characterId:string;className:string;level:number;instances:readonly EquipmentItemInstanceV24[];loadout:EquipmentLoadoutV24;}
const allStats=():Record<GearStatKey,number>=>({maxHp:0,power:0,armor:0,ward:0,accuracy:0,evasion:0,critRate:0,critDamage:0,haste:0,tenacity:0,penetration:0,potency:0});
export function validateEquipV24(ctx:EquipContextV24,itemId:string){const inst=ctx.instances.find(v=>v.id===itemId);if(!inst)throw new Error('item_not_owned');if(inst.characterId!==ctx.characterId)throw new Error('wrong_character');const p=EQUIPMENT_PIECES_V23.find(v=>v.id===inst.pieceId);if(!p)throw new Error('unknown_piece');if(p.className!==ctx.className)throw new Error('wrong_class');if(ctx.level<p.requiredLevel)throw new Error('level_too_low');return {inst,piece:p};}
export function equipItemV24(ctx:EquipContextV24,itemId:string):EquipmentLoadoutV24{const {piece}=validateEquipV24(ctx,itemId);const next={...ctx.loadout,[piece.slot]:itemId};const ids=Object.values(next).filter(Boolean);if(new Set(ids).size!==ids.length)throw new Error('same_item_multiple_slots');return next;}
export function loadoutSummaryV24(ctx:Omit<EquipContextV24,'loadout'>&{loadout:EquipmentLoadoutV24}){const stats=allStats(),equipped=[] as {pieceId:string;setId:string;slot:EquipmentSlot}[];for(const itemId of Object.values(ctx.loadout)){if(!itemId)continue;const {inst,piece}=validateEquipV24({...ctx,loadout:ctx.loadout},itemId);const d=deriveInstanceV24(inst);for(const [k,v] of Object.entries(d.stats) as [GearStatKey,number][])stats[k]=Number((stats[k]+v).toFixed(5));equipped.push({pieceId:piece.id,setId:piece.setId,slot:piece.slot});}const sets=resolveSetBonusesV22(equipped,EQUIPMENT_SETS_V23);return {stats,sets};}
export function compareEquipV24(ctx:EquipContextV24,candidateItemId:string){const {piece}=validateEquipV24(ctx,candidateItemId);const before=loadoutSummaryV24(ctx),afterLoadout=equipItemV24(ctx,candidateItemId),after=loadoutSummaryV24({...ctx,loadout:afterLoadout});const delta=allStats();for(const k of Object.keys(delta) as GearStatKey[])delta[k]=Number((after.stats[k]-before.stats[k]).toFixed(5));return {slot:piece.slot,currentItemId:ctx.loadout[piece.slot],candidateItemId,statDelta:delta,beforeSets:before.sets,afterSets:after.sets};}
