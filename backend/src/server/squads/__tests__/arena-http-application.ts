import {ArenaApplicationService} from '../arena-application';
import {ArenaHttpApplication} from '../arena-http-application';
import {MemoryArenaRuntimeRepository} from '../arena-persistence';
import type {AuthoritativeLoadoutRecord} from '../../coop/loadout-snapshots';
const ok=(value:unknown,message:string)=>{if(!value)throw new Error(message)};
const rows:AuthoritativeLoadoutRecord[]=['IRONWARDEN','WAYFINDER','DAWNKEEPER'].map((classId,index)=>({accountId:'a',characterId:`c${index+1}`,classId,loadoutId:'active',revision:1,characterLevel:25,dungeonUnlocked:true,legalEquipment:true,stats:{characterId:`c${index+1}`,classId,level:25,displayName:`c${index+1}`,maxHp:180,attackPower:70,healingPower:40,defense:55,accuracy:.9,evasion:.08,critChance:.1,haste:.05},abilities:[],capabilities:['damage','threat','defense','restore','mitigate','utility']}));
const service=new ArenaApplicationService({runtime:new MemoryArenaRuntimeRepository(),loadouts:{getActiveArenaLoadout:(accountId,characterId)=>rows.find(row=>row.accountId===accountId&&row.characterId===characterId)},serverSecret:'test-secret-0123456789'}),http=new ArenaHttpApplication(service),formation=rows.map((row,index)=>({characterId:row.characterId,position:(['front','middle','back'] as const)[index]}));
const run=async()=>{const defense=await http.publishDefense('a',{requestId:'publish-1234',formation,label:'Tester'},Date.UTC(2026,8,13));ok(defense.published,'defense publication');let rejected=false;try{await http.publishDefense('a',{requestId:'bad',formation:[formation[0],formation[0],formation[2]]},Date.UTC(2026,8,13))}catch{rejected=true}ok(rejected,'HTTP boundary accepted duplicate formation');let boundary=false;try{await http.entry('   ',Date.UTC(2026,8,13))}catch{boundary=true}ok(boundary,'blank account identity accepted');boundary=false;try{await http.entry('a',Number.NaN)}catch{boundary=true}ok(boundary,'invalid server time accepted');console.log('arena HTTP application tests passed')};void run();
