export type SharedWorldNotificationKey='regional_crisis_started'|'regional_crisis_stage'|'regional_crisis_secured'|'regional_crisis_failed'|'world_boss_spawned'|'world_boss_phase'|'world_boss_defeated'|'world_boss_ending_soon';
export interface SharedWorldNotification { key:SharedWorldNotificationKey; title:string; body:string; deepLink:string; dedupeKey:string; }
export function worldBossSpawnedNotification(input:{instanceId:string;bossName:string;regionId:string}):SharedWorldNotification{return {key:'world_boss_spawned',title:`World Boss: ${input.bossName}`,body:`A World Boss has appeared in ${input.regionId}. Your attempts are asynchronous; fight when it suits you.`,deepLink:`veldryn://world/world-boss/${input.instanceId}`,dedupeKey:`world-boss-spawned:${input.instanceId}`};}
export function crisisStageNotification(input:{instanceId:string;crisisName:string;stageName:string}):SharedWorldNotification{return {key:'regional_crisis_stage',title:`${input.crisisName}: ${input.stageName}`,body:'The server has reached a new regional crisis stage.',deepLink:`veldryn://world/crisis/${input.instanceId}`,dedupeKey:`crisis-stage:${input.instanceId}:${input.stageName}`};}
