import {EQUIPMENT_PIECES_V23} from './equipment-catalog-v23';import type {EquipmentSlot,GearStatKey} from './equipment-types-v22';import {aggregateStatLinesV24,baseStatsForPieceV24} from './equipment-stat-budget-v24';import {enchantAdditionV24,statGemAdditionV24,validateEffectGemV24} from './equipment-customization-v24';import {RARITY_STAT_MULT_V26,type CraftedRarityV26} from './equipment-craft-rarity-v26';
export type EquipmentAcquireSourceV26='craft'|'legacy_conversion'|'admin_repair'|'quest_grant'|'chase_recipe';
export interface EquipmentItemInstanceV26{id:string;characterId:string;pieceId:string;rarity:CraftedRarityV26;upgradeRank:number;enchantId?:string;enchantRank?:number;statGemId?:string;statGemRank?:number;effectGemId?:string;effectGemRank?:number;acquireSource:EquipmentAcquireSourceV26;createdAt:string;}
export interface DerivedInstanceV26{instance:EquipmentItemInstanceV26;slot:EquipmentSlot;pieceName:string;setId:string;stats:Record<GearStatKey,number>;rarityMultiplier:number;effectGemId?:string;}
export function deriveInstanceV26(v:EquipmentItemInstanceV26):DerivedInstanceV26{const piece=EQUIPMENT_PIECES_V23.find(p=>p.id===v.pieceId);if(!piece)throw new Error('unknown_piece');const rm=RARITY_STAT_MULT_V26[v.rarity];const base=baseStatsForPieceV24(piece,v.upgradeRank).lines.map(l=>({stat:l.stat,value:l.upgradedValue*rm}));const e=enchantAdditionV24(piece,v.enchantId,v.enchantRank),g=statGemAdditionV24(piece,v.statGemId,v.statGemRank);if(e)base.push(e);if(g)base.push(g);validateEffectGemV24(piece,v.effectGemId,v.effectGemRank);return {instance:v,slot:piece.slot,pieceName:piece.name,setId:piece.setId,stats:aggregateStatLinesV24(base),rarityMultiplier:rm,effectGemId:v.effectGemId};}
