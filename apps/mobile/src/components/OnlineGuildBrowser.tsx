import {GuildCrest} from './SocialIdentity';
import {GameTextInput as TextInput} from './GameTextInput';
import {useEffect,useState} from 'react';
import {Alert,StyleSheet,Text,View} from 'react-native';
import {GameButton} from './GameButton'; import {Panel} from './Panel'; import {C,typography} from '../theme/theme';
import {onlineConfigured} from '../online/supabase'; import {browseGuilds,createOnlineGuild,requestGuildMembership,type OnlineGuild} from '../online/social';
export function OnlineGuildBrowser(){const [guilds,setGuilds]=useState<OnlineGuild[]>([]),[busy,setBusy]=useState(false),[loaded,setLoaded]=useState(false),[name,setName]=useState(''),[showCreate,setShowCreate]=useState(false),[policy,setPolicy]=useState<'open'|'apply'>('open');const load=async()=>{if(!onlineConfigured)return;setBusy(true);try{setGuilds(await browseGuilds());setLoaded(true)}catch(error){Alert.alert('Guilds',error instanceof Error?error.message:'Unable to load guilds.')}finally{setBusy(false)}};useEffect(()=>{load()},[]);if(!onlineConfigured)return null;const join=async(guild:OnlineGuild)=>{setBusy(true);try{const result=await requestGuildMembership(guild.id);Alert.alert(result==='joined'?'Guild joined':'Application sent',result==='joined'?`You joined ${guild.name}.`:`${guild.name} will review your application.`);await load()}catch(error){Alert.alert('Guilds',error instanceof Error?error.message:'Unable to join guild.')}finally{setBusy(false)}};const create=async()=>{setBusy(true);try{await createOnlineGuild(name,policy,1);setName('');Alert.alert('Guild created','You are its leader. Sync your character first so your player name appears on the roster.');await load()}catch(error){Alert.alert('Create guild',error instanceof Error?error.message:'Unable to create guild.')}finally{setBusy(false)}};return <Panel><Text style={s.title}>Guild directory · online</Text><Text style={s.sub}>Browse published guilds from the server. Open guilds join immediately; application guilds review requests.</Text><GameButton title={showCreate?'Hide guild creation':'Create a guild'} tone="secondary" onPress={()=>setShowCreate(v=>!v)}/>{showCreate&&<><TextInput accessibilityLabel="New guild name" value={name} onChangeText={setName} maxLength={24} placeholder="New guild name" placeholderTextColor={C.muted} style={s.input}/><View style={s.actions}><View style={s.flex}><GameButton title="Open" tone={policy==='open'?'primary':'secondary'} disabled={busy} onPress={()=>setPolicy('open')}/></View><View style={s.flex}><GameButton title="Applications" tone={policy==='apply'?'primary':'secondary'} disabled={busy} onPress={()=>setPolicy('apply')}/></View></View><GameButton title="Create guild" disabled={busy||name.trim().length<3} onPress={create}/></>}<GameButton title={busy?'Loading…':'Refresh directory'} tone="secondary" disabled={busy} onPress={load}/>{loaded&&!guilds.length&&<Text style={s.empty}>No online guilds have been created yet.</Text>}{guilds.map(guild=><View key={guild.id} style={s.row}><GuildCrest size={40} bannerId={guild.banner_id}/><View style={s.copy}><Text style={s.name}>{guild.name}</Text><Text style={s.meta}>Level {guild.level} · {guild.minimum_level}+ required · {guild.join_policy==='open'?'Open':guild.join_policy==='apply'?'Application':'Invite only'}</Text><Text numberOfLines={1} style={s.motto}>{guild.motto}</Text></View><GameButton title={guild.join_policy==='open'?'Join':guild.join_policy==='apply'?'Apply':'Invite'} tone="secondary" disabled={busy||guild.join_policy==='invite'} onPress={()=>join(guild)}/></View>)}</Panel>}
const s=StyleSheet.create({title:{color:C.text,...typography.title,marginBottom:5},sub:{color:C.muted,lineHeight:19,marginBottom:8},input:{minHeight:44,borderWidth:1,borderColor:C.line,borderRadius:8,color:C.text,paddingHorizontal:10,marginBottom:8},actions:{flexDirection:'row',gap:8,marginBottom:8},flex:{flex:1},empty:{color:C.muted,fontSize:12,marginTop:8},row:{flexDirection:'row',gap:8,alignItems:'center',paddingVertical:10,borderTopWidth:1,borderColor:C.line},copy:{flex:1,minWidth:0},name:{color:C.text,fontWeight:'900'},meta:{color:C.muted,fontSize:12,marginTop:2},motto:{color:C.accent,fontSize:11,fontStyle:'italic',marginTop:2}});
