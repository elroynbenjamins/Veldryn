import type {MobileCraftQueueRowV31,MobileCraftRequirementV31} from './equipment-crafting-v31';
import {sourceActionLabelV31} from './equipment-crafting-v31';
export interface CraftingDetailPayloadV32{detail:{pieceId:string;title:string;targetTimeLabel:string;queueTimeLabel:string;canCraftNow:boolean;canQueuePrerequisites:boolean;primaryCta:'Craft'|'Craft prerequisites'|'View blockers';blockerCount:number};steps:readonly {id:string;kind:'process'|'component'|'final';label:string;quantity:number;seconds:number;dependsOn:readonly string[];status:'ready'|'blocked'}[];blockers:readonly {kind:string;key:string;label:string;missing:number;source:string;action:string}[];pve:readonly string[];}
export function craftingScreenModelV32(p:CraftingDetailPayloadV32){const requirements:MobileCraftRequirementV31[]=p.blockers.map(b=>({key:b.key,label:b.label,progressLabel:`Need ${b.missing}`,state:b.kind==='skill'||b.kind==='character_level'||b.kind==='recipe_unlock'?'blocked':'missing',source:b.source,actionLabel:sourceActionLabelV31(b.action)}));const queue:MobileCraftQueueRowV31[]=p.steps.map((s,i)=>({id:s.id,label:`${s.label}${s.quantity>1?` ×${s.quantity}`:''}`,kind:s.kind,status:s.status==='blocked'?'blocked':'pending',timeLabel:`${Math.ceil(s.seconds/60)}m`,dependencyCount:s.dependsOn.length}));return {header:p.detail,requirements,queue,pve:p.pve,sections:['Result','Requirements','Crafting chain','Sources','Set / Skin'] as const};}
