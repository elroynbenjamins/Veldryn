import type {AchievementRepository} from './achievement-persistence';
export class AchievementApplicationService{constructor(private readonly repository:AchievementRepository){}snapshot(accountId:string){if(!accountId?.trim())throw new Error('AUTH_REQUIRED');return this.repository.snapshot(accountId.trim())}claim(accountId:string,achievementId:string,requestId:string,nowMs:number){if(!accountId?.trim())throw new Error('AUTH_REQUIRED');if(!achievementId?.trim())throw new Error('ACHIEVEMENT_ID_REQUIRED');if(requestId.length<8||requestId.length>128)throw new Error('INVALID_REQUEST_ID');if(!Number.isFinite(nowMs)||nowMs<0)throw new Error('INVALID_TIME');return this.repository.claim(accountId.trim(),achievementId.trim(),requestId,nowMs)}setShowcase(accountId:string,ids:string[],requestId:string,nowMs:number){if(!accountId?.trim())throw new Error('AUTH_REQUIRED');if(requestId.length<8||requestId.length>128)throw new Error('INVALID_REQUEST_ID');if(new Set(ids).size!==ids.length||ids.length>3)throw new Error('ACHIEVEMENT_SHOWCASE_INVALID');if(!Number.isFinite(nowMs)||nowMs<0)throw new Error('INVALID_TIME');return this.repository.setShowcase(accountId.trim(),ids,requestId,nowMs)}}
