import {useEffect,useMemo,useRef,useState} from 'react';
import {Image,PanResponder,Pressable,StyleSheet,Text,View} from 'react-native';
import {WORLD_CHANNELS,worldMessages,type WorldMessage} from '../online/social';
import {uiIcons} from '../theme/ui-icons';
import {radii,typography,type ThemeColors} from '../theme/theme';
import {useGameTheme} from '../theme/ThemeContext';

export function ChatDock({enabled,onOpen,unreadCount=0,mentionCount=0}:{enabled:boolean;onOpen:()=>void;unreadCount?:number;mentionCount?:number}){
 const C=useGameTheme(),s=useMemo(()=>makeStyles(C),[C]);
 const [rows,setRows]=useState<WorldMessage[]>([]),[offset,setOffset]=useState({x:0,y:0});
 const offsetRef=useRef(offset),origin=useRef(offset);offsetRef.current=offset;
 const drag=useMemo(()=>PanResponder.create({onPanResponderGrant:()=>{origin.current=offsetRef.current},onMoveShouldSetPanResponder:(_,g)=>Math.abs(g.dx)>5||Math.abs(g.dy)>5,onPanResponderMove:(_,g)=>setOffset({x:origin.current.x+g.dx,y:origin.current.y+g.dy})}),[]);
 useEffect(()=>{if(!enabled)return;let active=true;const load=()=>void worldMessages(WORLD_CHANNELS[0].id).then(next=>{if(active)setRows(next.slice(-3));}).catch(()=>{});load();const timer=setInterval(load,8000);return()=>{active=false;clearInterval(timer)};},[enabled]);
 if(!enabled)return <Pressable accessibilityRole="button" accessibilityLabel="Open chat" onPress={onOpen} style={({pressed})=>[s.bubble,pressed&&s.pressed]}><Image source={uiIcons.chat} resizeMode="contain" style={s.icon}/></Pressable>;
 return <Pressable {...drag.panHandlers} accessibilityRole="button" accessibilityLabel={unreadCount>0?'Open chat, '+unreadCount+' unread messages':'Open chat'} accessibilityHint="Tap to open chat. Drag to move the chat preview." onPress={onOpen} style={({pressed})=>[s.dock,{transform:[{translateX:offset.x},{translateY:offset.y}]},pressed&&s.pressed]}>
  <View style={s.heading}><Image source={uiIcons.chat} resizeMode="contain" style={s.smallIcon}/><Text style={s.channel}>WORLD</Text>{mentionCount>0?<View accessibilityLabel={mentionCount+' unread chat mentions'} style={s.mentionBadge}><Text style={s.mentionText}>@{mentionCount>9?'9+':mentionCount}</Text></View>:null}{unreadCount>0?<View accessibilityLabel={unreadCount+' unread Guild or Party messages'} style={s.unreadBadge}><Text style={s.unreadText}>{unreadCount>99?'99+':unreadCount}</Text></View>:null}<Text style={s.open}>OPEN ›</Text></View>
  {rows.length?rows.map(row=><Text numberOfLines={1} key={row.id} style={s.message}><Text style={s.name}>{row.sender_name}: </Text>{row.body}</Text>):<Text style={s.empty}>World chat is quiet.</Text>}
 </Pressable>;
}
function makeStyles(C:ThemeColors){return StyleSheet.create({dock:{position:'absolute',left:10,bottom:82,zIndex:20,width:'58%',maxWidth:260,minHeight:72,paddingHorizontal:10,paddingVertical:8,backgroundColor:C.panel,borderLeftWidth:2,borderLeftColor:C.info,borderRadius:radii.sm},heading:{flexDirection:'row',alignItems:'center',gap:6,marginBottom:3},smallIcon:{width:16,height:16},channel:{...typography.caption,color:C.info,fontWeight:'900',letterSpacing:.8},mentionBadge:{minWidth:24,height:18,paddingHorizontal:4,alignItems:'center',justifyContent:'center',borderRadius:9,backgroundColor:C.warningSurface,borderWidth:1,borderColor:C.warning},mentionText:{fontSize:8,color:C.warning,fontWeight:'900'},unreadBadge:{minWidth:18,height:18,paddingHorizontal:4,alignItems:'center',justifyContent:'center',borderRadius:9,backgroundColor:C.notification},unreadText:{fontSize:8,color:C.notificationText,fontWeight:'900'},open:{...typography.caption,color:C.muted,marginLeft:'auto',fontSize:10},message:{color:C.text,fontSize:12,lineHeight:17,textShadowColor:C.dark?'#000':'transparent',textShadowRadius:C.dark?3:0},name:{color:C.info,fontWeight:'800'},empty:{...typography.caption,color:C.muted},bubble:{position:'absolute',left:12,bottom:82,zIndex:20,width:48,height:48,alignItems:'center',justifyContent:'center',backgroundColor:C.panelRaised,borderRadius:24,borderWidth:StyleSheet.hairlineWidth,borderColor:C.line},icon:{width:25,height:25},pressed:{opacity:.65}});}
