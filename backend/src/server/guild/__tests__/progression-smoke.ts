import {strict as assert} from 'node:assert';import {canAllocate,guildUpgradeEffects,spentPoints} from '../progression';
const ranks:Record<string,number>={};assert.equal(spentPoints(ranks),0);const skill={id:'project_coordination',branch:'community' as const,name:'Project Coordination',maxRank:3,costPerRank:[3,5,7],effectKey:'projectContributionEfficiencyBps' as const,effectPerRank:100};assert.equal(canAllocate(ranks,skill,1),true);const next={...ranks,[skill.id]:1};assert.equal(guildUpgradeEffects(next).projectContributionEfficiencyBps,100);assert.equal(canAllocate(next,skill,3),false);console.log('guild progression PASS');
