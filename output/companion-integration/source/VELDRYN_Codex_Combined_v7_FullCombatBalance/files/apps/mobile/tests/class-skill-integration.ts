import {CLASSES} from '../src/content/classes';
import {classSkillsFor} from '../src/content/class-skills';
import {MONSTERS} from '../src/content/monsters';
import {createCharacter,newGame,startCombat,startClassTraining,claimActivity,previewActivityReward,effectiveStats,captureClassTrainingSnapshot,offlineCapSeconds,challengeFallenKnight,startGathering,craftRecipe} from '../src/core/game';
import {normalizeClassSkills,characterClassSkills,MAX_CLASS_SKILL_XP,TRAINING_GROUND_TARGET} from '../src/core/class-skills';
import {setAccountTrainingFocus,settleAccountActivities,createAccountCharacter,switchAccountCharacter,transitionAccountActivity,grantAccountPurchase} from '../src/core/account-actions';
import {accountCharacters,accountSkillLevel,projectCharacter} from '../src/core/account-roster';
import {classSkillView} from '../src/core/class-skill-view';
import {activityCycleSeconds,activityRate} from '../src/core/dashboard';
import {migrateSave} from '../src/core/save-migrations';
import {createSaveBackup,parseSaveBackup} from '../src/core/save-transfer';
import {environmentEffect} from '../src/core/world-weather';
import {activityEventDrops} from '../src/core/live-events';
import {characterTotalXpAtLevel,totalXpAtLevel} from '../src/core/progression';
import {rewardHasProgress} from '../src/core/playability';
import type {GameState,ClassId} from '../src/core/types';
import {equal,ok,close,test,throws,skillLevels} from './account-test-helpers';
const T=Date.UTC(2026,8,10,12);
const fresh=(id:ClassId='IRONWARDEN')=>createCharacter(newGame(T),id,'Tester');
const fight=(id:ClassId='IRONWARDEN')=>startCombat(fresh(id),'MOSS_RAT',T);
const totalClass=(s:GameState)=>characterClassSkills(s.character!).reduce((sum,k)=>sum+k.xp,0);
const compact=(s:GameState)=>({character:s.character,skills:s.skills,items:s.inventory.stacks,bank:s.bank.stacks,remainder:s.rewardRemainders,completed:s.activity?.completedActions});
for(const cls of CLASSES){
 test(`${cls.name}: combat awards both skills without damage/healing counters`,()=>{const s=fight(cls.id),reward=previewActivityReward(s,T+activityCycleSeconds(s)*1000+1);equal(reward.kills,1,'one victory');ok(reward.classSkillXp!.every(x=>x.xp>0),'two shares');const after=claimActivity(s,T+activityCycleSeconds(s)*1000+1).state;equal(totalClass(after),reward.classSkillXp!.reduce((sum,x)=>sum+x.xp,0),'applied exactly');ok(after.character!.xp>0,'separate characterXP');});
 test(`${cls.name}: both skills actually improve runtime combat by mastery`,()=>{const s=fight(cls.id),base=captureClassTrainingSnapshot(s),baseStats=effectiveStats(s);s.character!.classSkills=classSkillsFor(cls.id).map(skill=>({skillId:skill.id,xp:MAX_CLASS_SKILL_XP,level:100}));s.activity!.classTrainingSnapshot=undefined;const trained=captureClassTrainingSnapshot(s);ok(trained.cycleSeconds<=base.cycleSeconds+1e-9,'no slower');ok(trained.damage<=base.damage,'no more damage');ok(trained.maxHp>=base.maxHp,'no lessHP');ok(trained.cycleSeconds<base.cycleSeconds||trained.recovery>base.recovery||trained.maxHp>base.maxHp||trained.damage<base.damage||effectiveStats(s).attack>baseStats.attack||effectiveStats(s).defense>baseStats.defense,'actual improvement');});
}
test('preview is pure and zero-time or incomplete encounters award no XP',()=>{const s=fight(),before=JSON.stringify(s);equal(totalClass(claimActivity(s,T).state),0,'zero');const p=previewActivityReward(s,T+activityCycleSeconds(s)*400);equal(p.kills,0,'incomplete');equal(p.classSkillXp!.reduce((sum,a)=>sum+a.xp,0),0,'no partial XP');equal(JSON.stringify(s),before,'pure');});
test('repeated claim at same or earlier time cannot duplicate XP',()=>{const first=claimActivity(fight(),T+60000).state,xp=totalClass(first);equal(totalClass(claimActivity(first,T+60000).state),xp,'same');equal(totalClass(claimActivity(first,T+50000).state),xp,'earlier');});
test('failed encounter yields no victory XP and stops safely',()=>{const s=fight();s.character!.currentHp=1;s.inventory.stacks=[];const result=claimActivity(s,T+60000);equal(result.reward.kills,0,'no victory');equal(totalClass(result.state),0,'no classXP');equal(result.state.activity,null,'stop');equal(result.state.character!.currentHp,1,'safe low HP');});
test('focus changes do not retroactively redirect offline XP',()=>{const s=fight(),time=T+300000;const expected=claimActivity(s,time);const focused=setAccountTrainingFocus(s,'primary',time);equal(focused.character!.classSkills,expected.state.character!.classSkills,'old work');equal(focused.character!.trainingFocus,'primary','new selection');});
test('partially completed encounter retains focus; following encounter uses new choice',()=>{let s=fight();const cycle=activityCycleSeconds(s);s=setAccountTrainingFocus(s,'primary',T+cycle*500);equal(s.activity!.classTrainingSnapshot!.focus,'balanced','in-flight');ok(classSkillView(s,'guardcraft').pendingFocus,'UI pending');const at=T+cycle*1000+1;const first=claimActivity(s,at);equal(first.reward.classSkillXp!.map(a=>a.xp),[7,7],'first old split');const next=claimActivity(first.state,at+activityCycleSeconds(first.state)*1000+1);equal(next.reward.classSkillXp!.map(a=>a.xp),[10,4],'next new split with fractional carry');});
test('focus before any elapsed work applies to first encounter',()=>{const s=setAccountTrainingFocus(fight(),'secondary',T),p=previewActivityReward(s,T+activityCycleSeconds(s)*1000+1);equal(p.classSkillXp!.map(a=>a.xp),[3,11],'new focus first');});
test('focus does not change current skill effects or weaken the character',()=>{const s=fresh();const before=effectiveStats(s),after=setAccountTrainingFocus(s,'secondary',T);equal(effectiveStats(after),before,'same combat stats');});
test('invalid focus and backward command clock fail atomically',()=>{const s=fight(),before=JSON.stringify(s);throws(()=>setAccountTrainingFocus(s,'fake' as any,T),'bad focus');throws(()=>setAccountTrainingFocus(s,'primary',T-1),'clock');equal(JSON.stringify(s),before,'unchanged');});
test('an account purchase cannot amplify already-started encounter XP',()=>{let s=fight();const cycle=activityCycleSeconds(s);s=grantAccountPurchase(s,'vip',T+cycle*500);const result=claimActivity(s,T+cycle*1000+1);equal(result.reward.classSkillXp!.reduce((sum,a)=>sum+a.xp,0),14,'old encounter');const next=previewActivityReward(result.state,T+cycle*2000+2);ok(next.classSkillXp!.reduce((sum,a)=>sum+a.xp,0)>14,'new encounter usesVIP');});
test('one long claim and many short claims give matching progression, HP, food and loot',()=>{const initial=fight('DAWNKEEPER');const long=claimActivity(initial,T+3600000).state;let short=initial;for(let sec=10;sec<=3600;sec+=10)short=claimActivity(short,T+sec*1000).state;equal(compact(short),compact(long),'whole versus chunks');close(short.activity!.progressFraction??0,long.activity!.progressFraction??0,'fraction');});
test('skill improvements are applied during offline simulation, not only on return',()=>{let s=fight('WAYFINDER');s.character!.classSkills=normalizeClassSkills('WAYFINDER',[{skillId:'marksmanship',xp:totalXpAtLevel(10)-1}]);s.activity!.classTrainingSnapshot=undefined;const before=captureClassTrainingSnapshot(s).cycleSeconds;const result=claimActivity(s,T+600000);ok(result.state.character!.classSkills![0].level>=10,'milestone earned');ok((result.reward.nextClassTrainingSnapshot??captureClassTrainingSnapshot(result.state)).cycleSeconds<before,'next fight faster');});
test('safe drills train both skills without restoring HP, spending food or earning combat/event rewards',()=>{const s=fresh('DAWNKEEPER');s.character!.currentHp=12;const started=startClassTraining(s,T);const result=claimActivity(started,T+60000);equal(result.reward.trainingActions,1,'drill');equal(result.reward.classSkillXp!.map(a=>a.xp),[4,4],'pool');equal([result.reward.xp,result.reward.gold,result.reward.kills,result.reward.foodConsumed],[0,0,0,0],'no other rewards');equal(result.state.character!.currentHp,12,'no healing');equal(result.state.inventory,s.inventory,'inventory');equal(result.reward.items,[],'no drops');equal(result.reward.eventDrops,undefined,'no event credit');ok(rewardHasProgress(result.reward),'summary shown');});
test('drills use normal offline cap and cannot repeatedly bank old excess absence',()=>{const s=startClassTraining(fresh(),T),cap=offlineCapSeconds(s);const result=claimActivity(s,T+10*86400000);equal(result.reward.trainingActions,cap/60,'one cap');equal(claimActivity(result.state,T+10*86400000).reward.xp,0,'repeat no progress');equal(totalClass(claimActivity(result.state,T+10*86400000).state),totalClass(result.state),'no replay');});
test('training weather is neutral in every tested season and does not qualify for events',()=>{for(const seasonId of ['spring','summer','autumn','winter'] as const)for(const weatherId of ['clear','rain','storm','snow'] as const){const e=environmentEffect('training',{seasonId,weatherId});equal([e.actionTimeMultiplier,e.xpMultiplier,e.itemMultiplier],[1,1,1],'neutral');}const s=startClassTraining(fresh(),T);equal(activityEventDrops(s,{xp:0,gold:0,items:[],kills:0,elapsedSeconds:3600},T+3600000),[],'no helper leak');});
test('a capped skill is explicit and both capped skills stop the training activity',()=>{const s=fresh();s.character!.classSkills=normalizeClassSkills('IRONWARDEN',classSkillsFor('IRONWARDEN').map(skill=>({skillId:skill.id,xp:MAX_CLASS_SKILL_XP-2})));const result=claimActivity(startClassTraining(s,T),T+120000);equal(result.reward.trainingActions,1,'only one required');equal(result.state.activity,null,'stopped');equal(result.reward.classSkillXp!.map(a=>[a.xp,a.cappedXp]),[[2,2],[2,2]],'cap clear');throws(()=>startClassTraining(result.state,T+120000),'cannot restart maxed');});
test('new safe training replaces and settles only this character activity',()=>{const s=fight();const result=transitionAccountActivity(s,T+60000,{kind:'training',id:TRAINING_GROUND_TARGET});ok(totalClass(result.state)>0,'old fight settled');equal(result.state.activity!.kind,'training','one lane');});
test('independent characters retain distinct class skills and selected focus',()=>{let s=skillLevels(fresh(),[50,50,50,50,50]);s=createAccountCharacter(s,'DAWNKEEPER','Other','female',T);s=setAccountTrainingFocus(s,'secondary',T);s=startClassTraining(s,T);s=switchAccountCharacter(s,'LOCAL_CHAR_1',T);s=startCombat(s,'MOSS_RAT',T);const result=settleAccountActivities(s,T+600000).state;const other=projectCharacter(result,'LOCAL_CHAR_2');equal(result.character!.trainingFocus,'balanced','first focus');equal(other.character!.trainingFocus,'secondary','other focus');ok(totalClass(result)>0&&totalClass(other)>0,'both progress');equal(characterClassSkills(other.character!).map(k=>k.skillId),['restoration','sanctity'],'right pair');equal(accountCharacters(result).length,2,'no extra actors');});
test('skill contribution can unlock an account slot without counting combat level twice',()=>{const s=skillLevels(fresh(),[49,49,49,49,42]);s.character!.classSkills=normalizeClassSkills('IRONWARDEN',[{skillId:'guardcraft',xp:totalXpAtLevel(4)-1}]);equal(accountSkillLevel(s),249,'239+4 extra professions+2 class+3+1');const result=claimActivity(startClassTraining(s,T),T+60000).state;ok(accountSkillLevel(result)>=250,'crossed');equal(result.account.unlockedCharacterSlots,2,'watermark recorded');});
test('v9 save and backup round trips preserve in-flight focus and fractional work',()=>{let s=setAccountTrainingFocus(fight(),'secondary',T+3000);const restored=migrateSave(JSON.parse(JSON.stringify(s)));equal(restored.version,13,'schema');equal(restored.activity!.classTrainingSnapshot,s.activity!.classTrainingSnapshot,'snapshot');equal(restored.character!.trainingFocus,'secondary','pending focus');equal(parseSaveBackup(createSaveBackup(s)).character!.classSkills,s.character!.classSkills,'backup');const a=claimActivity(s,T+120000),b=claimActivity(restored,T+120000);equal(a.reward.classSkillXp,b.reward.classSkillXp,'resumedXP');close(a.reward.nextProgressFraction!,b.reward.nextProgressFraction!,'resumed fraction');});
test('v7 development saves add level-one class skills; older client rejects newer formats',()=>{const raw=JSON.parse(JSON.stringify(fresh()));raw.version=7;delete raw.character.classSkills;delete raw.character.trainingFocus;const result=migrateSave(raw);equal(result.version,13,'promoted');equal(characterClassSkills(result.character!).map(skill=>skill.xp),[0,0],'not backfilled from combatXP');throws(()=>migrateSave({...result,version:14}),'future blocked');});
test('corrupt or foreign encounter snapshots are discarded rather than trusted',()=>{const raw=JSON.parse(JSON.stringify(fight()));raw.activity.classTrainingSnapshot.classId='DAWNKEEPER';equal(migrateSave(raw).activity!.classTrainingSnapshot,undefined,'foreign');raw.activity.classTrainingSnapshot.classId='IRONWARDEN';raw.activity.classTrainingSnapshot.cycleSeconds=0;equal(migrateSave(raw).activity!.classTrainingSnapshot,undefined,'zero-time');});
test('displayed cycle and rates use the exact running encounter snapshot',()=>{const s=fight();close(activityCycleSeconds(s),s.activity!.classTrainingSnapshot!.cycleSeconds,'cycle');const rates=activityRate(s),snapshot=s.activity!.classTrainingSnapshot!;equal(rates.classSkillXpPerHour,Math.floor(Math.floor(3600/snapshot.cycleSeconds)*snapshot.classSkillXp),'poolrate');close(classSkillView(s,'guardcraft').currentSkillXpPerAction,7,'skillshare');});
test('gathering and crafting never award class-skill XP',()=>{let s=fresh();s=startGathering(s,'GREENWOOD_TREE',T);const result=claimActivity(s,T+600000);equal(totalClass(result.state),0,'gathering');const raw=fresh();raw.inventory.stacks.push({itemId:'SILVERFIN',quantity:5});const crafted=craftRecipe(raw,'COOK_SILVERFIN',T);equal(totalClass(crafted),0,'crafting');});
test('Fallen Knight awards class XP once on first clear, not repeat button presses',()=>{let s=fresh();s.character!.level=25;s.character!.xp=characterTotalXpAtLevel(25);s.quests=s.quests.map(q=>({...q,status:'claimed'}));let won:ReturnType<typeof challengeFallenKnight>|undefined;for(let i=0;i<200;i++){const result=challengeFallenKnight(s,T+i);if(result.won){won=result;break;}}ok(won,'fixture can win');ok(totalClass(won.state)>=3000,'boss pool');equal(totalClass(challengeFallenKnight(won.state,T+999).state),totalClass(won.state),'repeat no XP');});
test('purchases before elapsed work apply to the first future encounter',()=>{const s=grantAccountPurchase(fight(),'vip',T);const snapshot=s.activity!.classTrainingSnapshot??captureClassTrainingSnapshot(s);ok(snapshot.classSkillXp>14,'future XP boosted');const result=claimActivity(s,T+snapshot.cycleSeconds*1000+1);ok(result.reward.classSkillXp!.reduce((sum,a)=>sum+a.xp,0)>14,'first reward uses new bonus');});
console.log('CLASS SKILL ENGINE / OWNERSHIP CHECKS COMPLETE');
