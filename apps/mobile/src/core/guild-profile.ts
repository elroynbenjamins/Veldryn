import {GameState} from './types';
import {normalizeGuildBannerId,normalizeGuildFrameId,normalizeGuildMotto,normalizeGuildNameplateId,type GuildBannerId,type GuildFrameId,type GuildNameplateId} from './guild-customization';
export interface GuildProfile{ id:string;name:string;emblem:string;bannerId:GuildBannerId;frameId:GuildFrameId;nameplateId:GuildNameplateId;motto:string;level:number;language:string;description:string;members:number;capacity:number;weeklyActivity:number;pveProgress:number;bossHp:number;joinPolicy:'open'|'apply'|'invite';minimumLevel:number; }
export function localGuildProfile(state:GameState):GuildProfile{
  return {id:'LOCAL_BLOOMWARDENS',name:'The Bloomwardens',emblem:'✦',bannerId:normalizeGuildBannerId(state.account.guildBannerId),frameId:normalizeGuildFrameId(state.account.guildProfileFrameId),nameplateId:normalizeGuildNameplateId(state.account.guildNameplateId),motto:normalizeGuildMotto(state.account.guildMotto),level:3,language:'English',description:'A friendly expedition guild focused on steady PvE progress.',members:5,capacity:12,weeklyActivity:68,pveProgress:state.account.guildProjectProgress??0,bossHp:state.account.guildBossHp??100000,joinPolicy:state.account.guildJoinPolicy??'open',minimumLevel:state.account.guildMinimumLevel??10};
}
