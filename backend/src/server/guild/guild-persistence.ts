import type {GuildChatMessage,GuildCreateInput,GuildDirectoryEntry,GuildInviteProjection,GuildLeaderboardEntry,GuildProfileUpdateInput,GuildSelfProjection,GuildRole} from './guild-types';
export interface GuildRepository{self(accountId:string):Promise<GuildSelfProjection|null>;directory(accountId:string,limit:number):Promise<GuildDirectoryEntry[]>;leaderboard(accountId:string,limit:number):Promise<GuildLeaderboardEntry[]>;create(accountId:string,input:GuildCreateInput,nowMs:number):Promise<GuildSelfProjection>;updateProfile(accountId:string,input:GuildProfileUpdateInput,nowMs:number):Promise<GuildSelfProjection>;allocateSkill(accountId:string,skillId:string,requestId:string,nowMs:number):Promise<GuildSelfProjection>;contributeProject(accountId:string,characterId:string,optionId:string,requestId:string,nowMs:number):Promise<GuildSelfProjection>;setRole?(accountId:string,targetAccountId:string,role:Exclude<GuildRole,'leader'>,requestId:string,nowMs:number):Promise<GuildSelfProjection>;removeMember?(accountId:string,targetAccountId:string,requestId:string,nowMs:number):Promise<GuildSelfProjection>;transferLeadership?(accountId:string,targetAccountId:string,requestId:string,nowMs:number):Promise<GuildSelfProjection>;leave?(accountId:string,requestId:string,nowMs:number):Promise<{left:boolean;guildDeleted:boolean}>;invite?(accountId:string,targetAccountId:string,requestId:string,nowMs:number):Promise<GuildInviteProjection>;myInvites?(accountId:string):Promise<GuildInviteProjection[]>;acceptInvite?(accountId:string,inviteId:string,requestId:string,nowMs:number):Promise<GuildSelfProjection>;chat?(accountId:string,limit:number):Promise<GuildChatMessage[]>;sendChat?(accountId:string,body:string,senderName:string,requestId:string,nowMs:number):Promise<GuildChatMessage>}
