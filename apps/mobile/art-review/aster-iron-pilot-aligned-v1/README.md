# Aster Iron — pilot-aligned proof

Status: visual-review candidate. Do not register as a runtime skin until approved.

These four views were regenerated with the built-in image generation tool using:

- the corresponding `assets/character-base-v1` image as the immutable identity, pose, scale, and backdrop reference;
- `assets/equipment-ui/aster-iron-progression-icon-sheet.png` as the clothing/material reference only.

## Acceptance rules

- Preserve the original pilot face, hair, head size, body proportions, stance, hands, ground line, pixel-art treatment, canvas, and backdrop.
- Change clothing only.
- Keep the helmet off so identity remains reviewable.
- Do not add a weapon, shield, cape, glow, checkerboard, text, or extra objects.

The generated output deliberately remains in `art-review` until a human approves all four views.
